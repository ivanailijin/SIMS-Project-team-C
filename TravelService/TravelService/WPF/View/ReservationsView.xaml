<UserControl x:Class="TravelService.WPF.View.ReservationsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TravelService.WPF.View"
        mc:Ignorable="d"
        Height="600" Width="700">
    <Grid>
        <TabControl Grid.Row="0" Height="560" VerticalAlignment="Top" Margin="0,20,0,0">
            <TabItem x:Name="ActiveReservations" Header="Active reservations">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="390"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                    <DataGrid x:Name="dataGridActiveReservations" Grid.Row="0"
                  ItemsSource="{Binding Path=ActiveReservations, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding Path = SelectedActiveReservation, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Width="700" Height="390">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Name" Binding="{Binding Accommodation.Name}" Width="170"></DataGridTextColumn>
                            <DataGridTextColumn Header="Location" Binding="{Binding Location.CityAndCountry}" Width="170"></DataGridTextColumn>
                            <DataGridTextColumn Header="Check-in" Binding="{Binding CheckInDate, StringFormat='{}{0:d}'}" Width="150"></DataGridTextColumn>
                            <DataGridTextColumn Header="Checkout" Binding="{Binding CheckOutDate, StringFormat='{}{0:d}'}" Width="150"></DataGridTextColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                    <StackPanel Grid.Row="1" Orientation="Horizontal">
                        <Separator Width="150" Background="White"></Separator>
                        <Button Margin="0,20,0,0" Width="120" Height="50" Command="{Binding SendRequestCommand}">
                            <TextBlock Text="Send request for delaying" TextWrapping="Wrap" TextAlignment="Center"></TextBlock>
                        </Button>
                        <Separator Width="100" Background="White"></Separator>
                        <Button Margin="0,20,0,0" Width="120" Height="50" Command="{Binding CancelReservationCommand}">
                            <TextBlock Text="Cancel reservation" TextWrapping="Wrap" TextAlignment="Center"></TextBlock>
                        </Button>
                    </StackPanel>
                </Grid>
            </TabItem>
            <TabItem x:Name="Req" Header="Requests for delaying">
                <DataGrid x:Name="dataGridReservationRequests"
                  ItemsSource="{Binding Path=RequestsForDelaying, UpdateSourceTrigger=PropertyChanged}"
                  AutoGenerateColumns="False" IsReadOnly="True"
                  Width="700" Height="480">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Name" Binding="{Binding Reservation.Accommodation.Name}" Width="90"></DataGridTextColumn>
                        <DataGridTextColumn Header="Location" Binding="{Binding Reservation.Location.CityAndCountry}" Width="100"></DataGridTextColumn>
                        <DataGridTemplateColumn Header="Old Dates" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                <Run Text="{Binding Reservation.CheckInDate, StringFormat='{}{0:d}'}"/>
                                <Run Text=" - "/>
                                <Run Text="{Binding Reservation.CheckOutDate, StringFormat='{}{0:d}'}"/>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTemplateColumn Header="New Dates" Width="150">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                <Run Text="{Binding NewStartDate, StringFormat='{}{0:d}'}"/>
                                <Run Text=" - "/>
                                <Run Text="{Binding NewEndDate, StringFormat='{}{0:d}'}"/>
                                    </TextBlock>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Status" Binding="{Binding StatusText}" Width="90"></DataGridTextColumn>
                        <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="120"></DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>

            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
