<Window x:Class="TravelService.WPF.View.SecondGuestView"

        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TravelService.WPF.View"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        Title="SecondGuestView" Height="450" Width="800">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <Button Width="300" Height="50" Command="{Binding TrackCommand}">Track tour</Button>
            <Button Width="300" Height="50" Command="{Binding TourViewCommand}">Tour view</Button>
            <Button Width="300" Height="50" Command="{Binding ReservationCommand}">Tour reservation</Button>
            <Button Width="300" Height="50" Command="{Binding VoucherViewCommand}">My Vouchers</Button>
            <Button Width="300" Height="50" Command="{Binding RateTourCommand}">Rate tours</Button>
        </StackPanel>

    </Grid>
</Window>
